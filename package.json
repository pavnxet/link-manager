{
  "name": "link-manager",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix worker run dev",
    "lint": "echo 'Telegram worker-only project: no root lint target'",
    "test": "npm --prefix worker run test",
    "build": "npm --prefix worker run build"
  }
}
